# TG Challenge Bot

Telegram-бот для проведения творческих челленджей в сообществах нейро-арта (Midjourney, Stable Diffusion, DALL-E, Flux и др.)

## Возможности

- Три типа челленджей: ежедневные, еженедельные, ежемесячные
- Голосование за темы: AI генерирует варианты, участники выбирают
- Простое участие: просто отправьте картинку в тему челленджа
- Подсчёт реакций: автоматический подсчёт (🌚 не учитывается)
- Лидерборд: рейтинг победителей
- Анти-плагиат: пересланные изображения не принимаются
- Умный AI: не повторяет темы (помнит последние 10)

---

## Установка

### 1. Создайте бота в Telegram

1. Найдите [@BotFather](https://t.me/BotFather)
2. `/newbot` → введите имя → введите username
3. Скопируйте токен: `1234567890:ABCdefGHI...`
4. `/mybots` → выберите бота → **Bot Settings** → **Group Privacy** → **Turn off**

### 2. Получите ID группы и топиков

1. Добавьте [@RawDataBot](https://t.me/RawDataBot) в группу
2. Скопируйте `chat_id` (например: `-1001234567890`)
3. ID топиков: откройте в Telegram Web, число после `/` в URL

### 3. Получите API-ключ Google AI

1. Откройте [Google AI Studio](https://aistudio.google.com/apikey)
2. Create API Key → скопируйте

### 4. Создайте Worker в Cloudflare

1. Войдите в [Cloudflare Dashboard](https://dash.cloudflare.com)
2. Workers & Pages → Create → Create Worker
3. Дайте имя (например: `tg-challenge-bot`)
4. Deploy → Edit Code
5. Удалите всё и вставьте содержимое `worker.js`
6. Deploy

### 5. Создайте KV хранилище

1. Workers & Pages → KV → Create namespace
2. Имя: `CHALLENGE_KV`
3. Скопируйте ID

### 6. Привяжите KV к Worker

1. Workers & Pages → ваш worker → Settings → Bindings
2. Add → KV Namespace
3. Variable name: `CHALLENGE_KV`
4. KV namespace: выберите созданный

### 7. Добавьте переменные

Workers → Settings → Variables and Secrets:

**Environment Variables:**
```
CHAT_ID = -1001234567890
TOPIC_DAILY = 123
TOPIC_WEEKLY = 456
TOPIC_MONTHLY = 789
TOPIC_WINNERS = 101
TIMEZONE_OFFSET = 3
BOT_LANGUAGE = ru
```

**Secrets (Encrypt):**
```
BOT_TOKEN = ваш_токен_бота
GEMINI_API_KEY = ваш_ключ_google_ai
```

### 8. Настройте Cron Triggers

Workers → Settings → Triggers → Cron Triggers → Add:

```
0 5 * * *      # Daily poll (08:00 МСК)
0 17 * * *     # Daily challenge (20:00 МСК)
0 10 * * 6     # Weekly poll (Сб 13:00 МСК)
0 17 * * 0     # Weekly challenge (Вс 20:00 МСК)
0 10 28 * *    # Monthly poll (28-е 13:00 МСК)
0 17 1 * *     # Monthly challenge (1-е 20:00 МСК)
```

### 9. Активируйте webhook

Откройте в браузере:
```
https://ваш-worker.workers.dev/setup
```

### 10. Добавьте бота в группу

Сделайте администратором с правами:
- Удалять сообщения
- Закреплять сообщения
- Управлять темами

---

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Приветствие и справка |
| `/help` | Справка по участию |
| `/stats` | Ваша статистика побед |
| `/leaderboard` | Топ-10 победителей |
| `/current` | Текущие активные челленджи |

### Админ-команды

| Команда | Описание |
|---------|----------|
| `/admin` | Панель управления |
| `/poll_daily` | Создать опрос дня |
| `/run_daily` | Запустить дневной челлендж |
| `/finish_daily` | Завершить дневной челлендж |
| `/status` | Состояние челленджей |

---

## Как это работает

1. **Генерация** — AI создаёт 4 темы, бот создаёт опрос
2. **Старт** — опрос закрывается, объявляется тема-победитель
3. **Участие** — отправляйте изображения, ставьте реакции
4. **Итоги** — подсчёт реакций, объявление победителя

---

## Лицензия

MIT
